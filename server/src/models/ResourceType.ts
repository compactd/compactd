enum ResourceTypes {
  AUDIO = 'audio',
  CLIP = 'clip',
  ARTWORK = 'artwork',
  METADATA = 'metadata',
  IGNORED = 'ignored',
  PLAYLIST = 'playlist',
  UNKNOWN = 'unknown'
}

export default ResourceTypes;
